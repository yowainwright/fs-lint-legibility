# Changelog

## 0.1.0 - Unreleased

- Add the dependency-free C17 policy library and `fs-lint` binary.
- Add strict JSON configuration discovery and new-file allow patterns.
- Add path, NUL-stream, staged Git, and merge-base checks.
- Add text and JSON diagnostics safe for arbitrary filename bytes.
- Add installable CMake targets and binary end-to-end coverage.
